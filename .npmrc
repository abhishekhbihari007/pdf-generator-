# Puppeteer configuration - skip Chromium download during install
# This is handled via environment variables in vercel.json
# For Vercel, also set SKIP_PUPPETEER_DOWNLOAD=1 in Project Settings > Environment Variables
